# create a class that performs the following operations on an array_ display data in a file or on the screen,
# flip data, find maximum an minimum. The class can get a set of values from a keyboard or a file.
# Use the strategy pattern and other necessary patterns.

# pouzijeme strategy pattern a na akci to rozjedeme

class DisplayStrategy:
    def action(self,data):
        pass

class PrintStrategy(DisplayStrategy):
    def action(selfself, data):

class FileStrategy()